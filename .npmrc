# Flat, npm-style node_modules so packages that rely on transitively-installed deps (e.g. NestJS's
# rxjs) keep resolving after the npm→pnpm migration. Revisit (strict isolated linker) once deps are
# fully declared.
node-linker=hoisted
