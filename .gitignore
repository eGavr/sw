build

node_modules

.env

# Static ffmpeg binaries served to environments for video recording (large, fetched, not source).
**/bin/ffmpeg/ffmpeg-*
# NetBridge forwarder binaries served to environments (built from packages/netbridge/forwarder).
**/bin/netbridge/netbridge-*
.dev-storage/
