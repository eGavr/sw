// The cloud made of the user's own machines: attached by hand (or ordered by us on their behalf), run
// by our machine agent, and sliced by the machine pool exactly like leased metal. The one cloud type
// whose inventory the control plane keeps itself.
export const selfHostedCloudType = "self-hosted";
