export const dockerLabels = {
    provider: "sw.provider",
    environmentId: "sw.environment.id",
    accountId: "sw.account.id",
    data: "sw.environment.data",
};

export const dockerProviderValue = "docker";
